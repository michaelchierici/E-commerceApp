import React from 'react';
import {CartItems} from '../../components/CartItems';
import {Container} from '../../style/base';

const Cart = () => {
  return (
    <Container>
      <CartItems />
    </Container>
  );
};

export default Cart;
